[project]
name = "new_hack"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "username", email = "''"},
]
dependencies = ["fastapi>=0.120.3", "uvicorn>=0.38.0"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
